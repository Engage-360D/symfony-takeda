{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "type": "object",

  "properties": {
    "data": {
      "type": "object",

      "properties": {
        "title": {
          "type": "string"
        },

        "content": {
          "type": "string"
        },

        "isActive": {
          "type": "boolean"
        },

        "createdAt": {
          "type": "string",
          "format": "date-time"
        },

        "links": {
          "type": "object",

          "properties": {
            "category": {
              "type": "string"
            }
          },

          "additionalProperties": false,

          "required": [
            "category"
          ]
        }
      },

      "additionalProperties": false,

      "required": [
        "title",
        "content",
        "isActive",
        "createdAt",
        "links"
      ]
    }
  },

  "required": [
    "data"
  ]
}